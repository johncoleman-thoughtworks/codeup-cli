schemaVersion: 1
id: copy-paste-programming-e5cb43c8c461
category: copy-paste-programming
severity: medium
status: unconfirmed
priority: medium
location:
  file: crates/codeup/src/llm/anthropic.rs
  line: 50
  contentHash: 5d2add5ffd6c357ec82ea8e6cbac17dacf878b6b1bd5af4ae74b499b71968c08
explanation: The entire retry loop (lines 50–89) is structurally identical to the one in `github_models.rs` — same `attempt` counter, same `should_retry` branch, same `retry_after_seconds`/`backoff_seconds` dispatch, same `tracing::warn!` format, same `bytes` extraction and error preview pattern. The `github_models.rs` neighbor even has a comment explicitly calling this out ("Same retry shape as the Anthropic client"). Both copies must be maintained in lockstep; a bug fix or policy change (e.g. changing the warn format, the error preview length, or adding jitter) must be applied to both files independently.
suggestedRemediation: 'Extract the retry loop into the shared `retry.rs` module as a generic async helper, e.g. `retry::execute(http_call: impl Fn() -> Future<Output=Result<Response>>) -> Result<Bytes>`. Both `AnthropicClient::analyze` and `GithubModelsClient::analyze` would then call this helper, passing only their provider-specific request-building closure. The logging label ("Anthropic" vs "GitHub Models") can be a parameter. The `retry.rs` module already has `MAX_ATTEMPTS`, `MAX_BACKOFF_SECONDS`, `should_retry`, `backoff_seconds`, and `retry_after_seconds` — it''s the natural home for this logic.'
detectedAt: "2026-06-21T11:29:50.686Z"
detectedBy: claude-sonnet-4-6
confidence: 0.9
history:
- timestamp: "2026-06-21T11:29:50.686Z"
  event: detected
