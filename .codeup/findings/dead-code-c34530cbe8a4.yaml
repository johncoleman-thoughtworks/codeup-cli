schemaVersion: 1
id: dead-code-c34530cbe8a4
category: dead-code
severity: low
status: unconfirmed
priority: low
location:
  file: crates/codeup-core/src/scanner/mod.rs
  line: 8
  endLine: 8
  contentHash: a9fcccd25e76f3f13258b951e2048948d2cb18596badaf16c0aea00f5f231a6e
explanation: The `pub use walk::{scan_workspace, FileEntry, ProjectIndex}` re-export flattens the public API of this module, but `graph` and `imports` are also declared `pub mod` (lines 3–5), which exposes their entire internal surfaces — including all internal helpers like `resolve_jvm`, `resolve_js`, `normalize_posix`, `jvm_imports`, etc. — as part of the crate's public API. If the intent is only to expose `scan_workspace`, `FileEntry`, and `ProjectIndex` (as the explicit re-export suggests), then `pub mod graph` and `pub mod imports` leak far more than intended and may constitute unused/unintentional public surface.
suggestedRemediation: Change `pub mod graph` and `pub mod imports` to `mod graph` and `mod imports` (crate-private) unless consumers genuinely need to call into these modules directly. This aligns the visibility with the explicit re-export on line 8 and avoids accidental API stabilisation of internal helpers.
detectedAt: "2026-06-21T10:20:27.139Z"
detectedBy: claude-sonnet-4-6
confidence: 0.6
history:
- timestamp: "2026-06-21T10:20:27.139Z"
  event: detected
