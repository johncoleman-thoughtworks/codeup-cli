schemaVersion: 1
id: dead-code-b1ac99d31068
category: dead-code
severity: low
status: unconfirmed
priority: low
location:
  file: crates/codeup-core/src/scanner/mod.rs
  line: 3
  contentHash: a9fcccd25e76f3f13258b951e2048948d2cb18596badaf16c0aea00f5f231a6e
explanation: The `graph` and `imports` sub-modules are declared public (`pub mod graph` and `pub mod imports`), but neither is re-exported from the module's public surface via a `pub use` statement the way `walk` items are. If callers only consume this crate through its top-level `lib.rs`, and `lib.rs` only re-exports `scan_workspace`, `FileEntry`, and `ProjectIndex` from this module, the entire `graph` and `imports` APIs are effectively unreachable from outside the crate unless something explicitly paths into them. Worth verifying whether the `pub` visibility on these two mod declarations is intentional or leftover from an earlier design.
suggestedRemediation: Audit whether `graph` and `imports` are referenced externally. If they are only used internally within the `scanner` module, change them to `mod graph` and `mod imports` (crate-private). If they are part of the intended public API, add explicit `pub use graph::...` / `pub use imports::...` re-exports here to make the intended surface clear and discoverable.
detectedAt: "2026-06-21T11:29:50.686Z"
detectedBy: claude-sonnet-4-6
confidence: 0.4
history:
- timestamp: "2026-06-21T11:29:50.686Z"
  event: detected
